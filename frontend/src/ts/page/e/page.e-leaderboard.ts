export enum GameMode {
    Sprint = "sprint",
    Ranked = "ranked",
    Score = "score",
}

export enum LeaderboardCategory {
    Global = "global",
    National = "national",
    Friends = "friends",
}

export enum SortDirection {
    Asc = "asc",
    Desc = "desc",
}