export enum GameMode {
    Sprint = "sprint",
    Ranked = "ranked",
}

export enum LeaderboardCategory {
    Global = "global",
    National = "national",
    Friends = "friends",
}

export enum SortDirection {
    Asc = "asc",
    Desc = "desc",
}